<!-- @TODO -->
<!-- <script lang="ts">
	import { themeManager } from '../stores/theme.store';

	const handleClick = () => {
		$themeManager?.toggleTheme();
	};
</script>

<button
	class="theme-toggle"
	type="button"
	onclick={handleClick}
	aria-label={`Current theme: ${$themeManager?.theme}. Click to switch.`}
>
	{$themeManager?.theme}
</button>

<style lang="scss">
	.theme-toggle {
		text-transform: capitalize;
		color: var(--color-foreground-03);
		background: transparent;
		border: none;
		padding: 0;
		font: inherit;
		cursor: pointer;

		@media (hover: hover) and (pointer: fine) {
			&:hover {
				color: var(--color-foreground-01);
			}
		}
	}
</style> -->
